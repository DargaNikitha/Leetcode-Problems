class Solution(object):
    def majorityElement(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        val=0
        c=0
        for num in nums:
            if c==0:
                val=num
            if num==val:
                c+=1
            else:
                c-=1
        return val
